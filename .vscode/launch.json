{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C file (build+run)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/build/${fileBasenameNoExtension}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "preLaunchTask": "build current C file (no debug)",
      "MIMode": "lldb",
      "externalConsole": false
    }
  ]
}
